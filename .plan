1. Replace current queue data structure with explicit slot model (array of objects with actor or reservation), ensuring creation, sanitization, and spacing utilities operate on slots.\n2. Update movement flow: spawning assigns to next slot, noticing relies on slot vacancy, advancement reserves target slot and animates toward computed anchors, arrival fills slot and clears reservations.\n3. Restore periodic idle/walk pose cycling for queued actors and keep code cleanup (remove unused helpers, ensure render uses slot anchors).
